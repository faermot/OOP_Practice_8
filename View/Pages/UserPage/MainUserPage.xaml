<Page x:Class="CW.FreelanceWork.View.Pages.UserPage.MainUserPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      mc:Ignorable="d"
      d:DesignHeight="550" d:DesignWidth="1000"
      FontFamily="Consolas">

    <Grid>
        <Border HorizontalAlignment="Center" VerticalAlignment="Center"
                CornerRadius="20" Width="800" Height="430"
                Background="#4D3825"
                Effect="{StaticResource MaterialDesignElevationShadow16}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="60"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Side menu -->
                <Border Grid.Column="0" Width="60" Height="430"
                        Background="#D4A84F" CornerRadius="20 0 0 20"
                        HorizontalAlignment="Left">
                    <ListView x:Name="ListViewMenu" Margin="0 80 0 0"
                              FontSize="10" SelectedIndex="0"
                              Foreground="White" Background="Transparent"
                              BorderThickness="0">

                        <ListViewItem Cursor="Hand" ToolTip="Уведомления" IsEnabled="False">
                            <StackPanel>
                                <materialDesign:PackIcon Kind="Bell"
                                    VerticalAlignment="Center" Margin="5"
                                    Foreground="#4D3825"/>
                            </StackPanel>
                        </ListViewItem>

                        <ListViewItem Cursor="Hand" ToolTip="Настройки" IsEnabled="False">
                            <StackPanel>
                                <materialDesign:PackIcon Kind="Settings"
                                    VerticalAlignment="Center" Margin="5"
                                    Foreground="#4D3825"/>
                            </StackPanel>
                        </ListViewItem>

                        <ListViewItem Cursor="Hand" ToolTip="Архив" IsEnabled="False">
                            <StackPanel>
                                <materialDesign:PackIcon Kind="Archive"
                                    VerticalAlignment="Center" Margin="5"
                                    Foreground="#4D3825"/>
                            </StackPanel>
                        </ListViewItem>

                        <ListViewItem Cursor="Hand" ToolTip="Баланс" IsEnabled="False">
                            <StackPanel>
                                <materialDesign:PackIcon Kind="CurrencyBitcoin"
                                    VerticalAlignment="Center" Margin="5"
                                    Foreground="#4D3825"/>
                            </StackPanel>
                        </ListViewItem>

                        <ListViewItem Cursor="Hand" ToolTip="Доска объявлений" IsEnabled="False">
                            <StackPanel>
                                <materialDesign:PackIcon Kind="OrderBoolAscending"
                                    VerticalAlignment="Center" Margin="5"/>
                            </StackPanel>
                        </ListViewItem>

                        <ListViewItem Cursor="Hand" ToolTip="Выйти"
                                      MouseDoubleClick="ListViewItem_MouseDoubleClick">
                            <StackPanel>
                                <materialDesign:PackIcon Kind="Logout"
                                    VerticalAlignment="Center" Margin="5"
                                    Foreground="#4D3825"/>
                            </StackPanel>
                        </ListViewItem>
                    </ListView>
                </Border>

                <!-- Orders DataGrid -->
                <StackPanel Grid.Column="1" HorizontalAlignment="Center"
                            VerticalAlignment="Center" Margin="8 0 8 0">
                    <DataGrid x:Name="DataOrderInfo"
                              FontSize="13" Height="400" Width="700"
                              AutoGenerateColumns="False"
                              IsReadOnly="True"
                              CanUserSortColumns="False"
                              Background="#D4A84F"
                              RowBackground="White"
                              AlternatingRowBackground="#FFF0F0"
                              GridLinesVisibility="Horizontal"
                              ColumnHeaderHeight="44"
                              CanUserAddRows="False"
                              CanUserDeleteRows="False"
                              CanUserResizeRows="False"
                              Effect="{StaticResource MaterialDesignElevationShadow16}">
                        <DataGrid.Columns>
                            <DataGridTextColumn Width="*" Header="ФИРМА"
                                               Binding="{Binding Employer.FIO}"
                                               FontSize="13"/>
                            <DataGridTextColumn Width="*" Header="ЗАКАЗ"
                                               Binding="{Binding Order}"
                                               FontSize="13"/>
                            <DataGridTemplateColumn>
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Button x:Name="BtnOk" Click="BtnOk_Click"
                                                Background="#D4A84F"
                                                Foreground="#4D3825"
                                                ToolTip="Оставить заявку на выполнение"
                                                BorderBrush="#D4A84F"
                                                Style="{StaticResource MaterialDesignFlatLightButton}"
                                                Padding="4">
                                            <StackPanel Orientation="Horizontal">
                                                <materialDesign:PackIcon Kind="Check" Width="16" Height="16"/>
                                            </StackPanel>
                                        </Button>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Page>
