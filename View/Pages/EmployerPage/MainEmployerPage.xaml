<Page x:Class="CW.FreelanceWork.View.Pages.EmployerPage.MainEmployerPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      mc:Ignorable="d"
      d:DesignHeight="550" d:DesignWidth="1000"
      FontFamily="Consolas">

    <Grid>
        <Border HorizontalAlignment="Center" VerticalAlignment="Center"
                CornerRadius="20" Width="580" Height="420"
                Background="#4D3825"
                Effect="{StaticResource MaterialDesignElevationShadow16}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="90"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" HorizontalAlignment="Center" VerticalAlignment="Center">
                    <TextBlock Text="Проверка информации о компании, введите необходимую информацию"
                               FontSize="18" TextWrapping="Wrap" Width="460"
                               Foreground="White" TextAlignment="Center" FontWeight="SemiBold"/>
                </StackPanel>

                <StackPanel Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Top"
                            Margin="0 10 0 0">

                    <!-- Company name -->
                    <StackPanel Orientation="Horizontal" Margin="0 0 0 8">
                        <materialDesign:PackIcon Kind="Domain" Margin="0 10 6 0" Foreground="#D4A84F"/>
                        <TextBox x:Name="TbCompanyName" Width="240" FontSize="14"
                                 Foreground="#D4A84F"
                                 materialDesign:HintAssist.Hint="Введите название компании"
                                 materialDesign:TextFieldAssist.HasClearButton="True"/>
                    </StackPanel>

                    <!-- INN -->
                    <StackPanel Orientation="Horizontal" Margin="0 0 0 8">
                        <materialDesign:PackIcon Kind="FileDocument" Margin="0 10 6 0" Foreground="#D4A84F"/>
                        <TextBox x:Name="TbINN" Width="240" FontSize="14"
                                 Foreground="#D4A84F"
                                 materialDesign:HintAssist.Hint="Введите ИНН"
                                 materialDesign:TextFieldAssist.HasClearButton="True"/>
                    </StackPanel>

                    <!-- CEO FIO -->
                    <StackPanel Orientation="Horizontal" Margin="0 0 0 16">
                        <materialDesign:PackIcon Kind="Email" Margin="0 10 6 0" Foreground="#D4A84F"/>
                        <TextBox x:Name="TbCeoFio" Width="240" FontSize="14"
                                 Foreground="#D4A84F"
                                 materialDesign:HintAssist.Hint="Введите ФИО ген. директора"
                                 materialDesign:TextFieldAssist.HasClearButton="True"/>
                    </StackPanel>

                    <!-- Check button -->
                    <StackPanel HorizontalAlignment="Center" Margin="0 0 0 12">
                        <Button x:Name="BtnCheck" Click="BtnCheck_Click"
                                Background="#D4A84F" Foreground="White"
                                BorderThickness="0"
                                Style="{StaticResource MaterialDesignRaisedButton}">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="Check" Margin="0 0 5 0"/>
                                <TextBlock Text="Проверить" FontWeight="SemiBold"/>
                            </StackPanel>
                        </Button>
                    </StackPanel>

                    <!-- Bottom action buttons -->
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                        <Button x:Name="BtnLoginEmploer" Click="BtnLoginEmploer_Click"
                                IsEnabled="False"
                                Background="#D4A84F" Foreground="White"
                                BorderThickness="0" Margin="0 0 10 0"
                                Style="{StaticResource MaterialDesignRaisedButton}">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="LoginVariant" Margin="0 0 5 0"/>
                                <TextBlock Text="Войти" FontWeight="SemiBold"/>
                            </StackPanel>
                        </Button>

                        <Button x:Name="BtnBack" Click="BtnBack_Click"
                                Background="#D4A84F" Foreground="White"
                                BorderThickness="0"
                                Style="{StaticResource MaterialDesignRaisedButton}">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="Logout" Margin="0 0 5 0"/>
                                <TextBlock Text="Начать работать" FontWeight="SemiBold"/>
                            </StackPanel>
                        </Button>
                    </StackPanel>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Page>
